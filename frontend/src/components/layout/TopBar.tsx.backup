export default function TopBar() {
  return (
    <header className="h-12 border-b px-4 flex items-center justify-between bg-white" role="banner">
      <div className="font-semibold" role="heading" aria-level={1}>ComputerPOS</div>
      <nav className="flex items-center gap-3 text-sm" role="navigation" aria-label="Thanh công cụ">
        <div 
          className="inline-flex items-center gap-1 text-green-600"> 
          role="status" 
          aria-label="Trạng thái kết nối: Online"
        >
          <span className="w-2 h-2 rounded-full bg-green-500" aria-hidden="true"></span>
          Online
        </div>
        <button 
          className="px-2 py-1 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors hover:bg-gray-50">
          aria-label="Mở tìm kiếm"
          type="button"
        >
          Tìm
        </button>
        <button 
          className="px-2 py-1 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors hover:bg-gray-50">
          aria-label="Xem thông báo"
          type="button"
        >
          Thông báo
        </button>
        <button 
          className="px-2 py-1 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors hover:bg-gray-50">
          aria-label="Menu tài khoản"
          type="button"
        >
          Tài khoản
        </button>
      </nav>
    </header>
  )
}


